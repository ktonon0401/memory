winget install --id Git.Git -e --source winget
pip install -r requirements.txt
cd Downloads && git clone https://github.com/ktonon0401/memory.git && cd memory && type python zi.py
rmdir memory /q /s
